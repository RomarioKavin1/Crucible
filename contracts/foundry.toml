[profile.default]
src = "src"
out = "out"
test = "test"
script = "script"
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
fs_permissions = [{ access = "read-write", path = "./" }]

[rpc_endpoints]
galileo = "${OG_GALILEO_RPC}"
mainnet = "${OG_MAINNET_RPC}"
